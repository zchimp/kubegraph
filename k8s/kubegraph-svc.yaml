apiVersion: v1
kind: Service
metadata:
  name: kubegraph-svc
  namespace: kubegraph
  labels:
    app: kubegraph
spec:
  type: NodePort
  selector:
    app: kubegraph
  ports:
    - port: 8080        
      targetPort: http
      nodePort: 31080
      protocol: TCP